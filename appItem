import './index.css'

// Write your code here
const AppItem = props => {
  const {itemDetails} = props

  return (
    <li className="cont">
      <img
        className="image"
        src={itemDetails.imageUrl}
        alt={itemDetails.appName}
      />
      <p>{itemDetails.appName}</p>
    </li>
  )
}

export default AppItem
